# SAKA QMS Frontend

Run the Spring Boot API on port `8080`, then start the React application:

```powershell
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`. Sign in, search for an item using at least three characters, select it, enter comments, and use **Print A4** to print the checklist.
